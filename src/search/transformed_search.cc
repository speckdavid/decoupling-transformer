#include "transformed_search.h"


using namespace std;

namespace transformed_search {
TransformedSearch::TransformedSearch(const plugins::Options &opts) : SearchAlgorithm(opts) {}

TransformedSearch::~TransformedSearch() {}
}
